import React from 'react';

const Form = () => {
    return (
        <div className={classes.formBackground}>
            <div className={classes.form}> </div>
        </div>
    );
};

export default Form;