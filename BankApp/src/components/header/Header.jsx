import React from 'react';
import classes from './header.module.sass'

const Header = () => {
    return (
        <header>
            <input type="text"/>
        </header>
    );
};

export default Header;